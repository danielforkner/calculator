const buttons = document.querySelectorAll('.calcBtn');
buttons.forEach(btn => {
    btn.addEventListener('click', (e) => updateDisplay(e.target.id));
});

const updateDisplay = function(id) {
    switch (id) {
        case 'btn1':
    };
} 